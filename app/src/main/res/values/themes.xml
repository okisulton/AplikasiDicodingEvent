<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DicodingFundamentalAndroidSubs1" parent="Theme.Material3.DynamicColors.Light.NoActionBar">

    <!-- Draw content into system bars. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.DicodingFundamentalAndroidSubs1" parent="Base.Theme.DicodingFundamentalAndroidSubs1" />
</resources>